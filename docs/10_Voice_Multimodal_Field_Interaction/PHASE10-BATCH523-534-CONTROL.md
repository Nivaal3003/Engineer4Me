# Phase 10 Batch 523–534: transcript provenance and revision review

This source-only batch extends manual/scripted field-interaction contracts after the accepted one-frame signal-presence proof. It does not transcribe the earlier frame or repeat the microphone check. The historical `signal_absent` receipt contains no words and is not a diagnosis of microphone failure or proof of speech absence.

## Review behavior
Text is preserved exactly; bounds and control-character rejection do not normalize negation, numbers, units, line breaks, or punctuation. Attachment identities are declared metadata, not verified content. Only objects issued in this memory-only session can enter the revision review. Text and attachment edits require another review, invalidate prior review/preview tokens, and cannot preserve approval. Review approval permits local preview only; no authenticated approval, engineering approval, operation allocation, or execution is implied.

At most sixteen reviews are held in the owned registry. Discard releases registry references and invalidates previews; it cannot guarantee erasure of immutable JavaScript strings or references retained by callers. No persistence or active application input UI is introduced.

## Future processing gate
Provider selection, licensing, privacy, data location, retention/deletion, language/quality, cancellation, security, fresh processing consent, and explicit execution approval require separate evidence. Caller-supplied `true` flags do not activate processing. Browser, microphone, sample access, recording, transmission, speech-to-text, voice-command interpretation, local/external AI, authentication, backend/protected content, service workers/PWA, native packaging, and deployment remain closed.

## Host controls
Use exact parent archive and receipt validation before repository/network activity. Preserve existing evidence and the main repository's ignored node_modules. Verification runs in an independent temporary clone. Build artifacts are bounded, observed, and cleaned only in that candidate. Unknown source/index/path changes fail closed. Committed content is read by Git object ID, avoiding long revision:path operands. Publication uses an exact lease and synchronization is fast-forward only.

## Evidence distinction
Embedded fixtures and pre-delivery tests are synthetic. Only a successful controlled host run establishes this batch's repository acceptance. The accepted parent console is real host evidence; its archive and receipt must still be independently validated from the user's Downloads.
